---
epic: 03
titulo: Capas analíticas en el mapa principal
estado: en-ejecucion
autor: Tomás (con Claude · Cowork)
fecha: 2026-05-18
revision: 2026-05-21 (r12) — Specs 46 (sistema de leyenda y onboarding visual) y 47 (tooltip enriquecido e interacción multi-capa) creadas en borrador-r1 en sesión de Cowork. Spec 46 r1 cierra 15 decisiones técnicas con onboarding como eje central (guided carousel modal de 4 pantallas: bienvenida → 4 capas → modelo de tiempo → despedida), chip persistente de glosario climático↔político en esquina inferior izquierda, refinamientos r2 a la leyenda (badge de calidad de 3 estados oficial/estimado/congelado, microcopy didáctico debajo de la escala, accordion `[▼ Cómo se lee esta capa]` con `shortIntro`), extensión del contrato `Layer` con dos campos opcionales nuevos (`legendMicrocopy: string` y `shortIntro: string`); botón `[ⓘ Cómo se lee este mapa]` al pie de la leyenda como único punto de re-entrada del overlay. Spec 47 r1 cierra 16 decisiones técnicas **confirmando single-select como ancla del epic** (cierra la decisión condicional #4 sin romperla) y resolviendo la necesidad multi-capa vía tooltip enriquecido con snapshot cruzado (`getCrossLayerSnapshot(slug, sliderDate)`), panel pineado persistente debajo de la leyenda flotante, y bloque desplegable "Otras capas para este país" en el header del drawer con links `[Cambiar a esta capa →]` que mantienen el drawer del mismo país abierto. Comparación país×país queda explícitamente fuera de scope r1 por decisión de Tomás. Spec 47 también extiende el contrato con `formatCrossLayerChip?(value): LayerChipFormat` opcional + helper `getCrossLayerSnapshot` reutilizable (futura Spec 48 risk management lo consume). Cada spec deja 1 decisión visual abierta (Anexo A) para sesión de Product Design separada (#16 header glyph compuesto del overlay; #17 diferenciación visual del chip cruzado). El epic queda con 4 specs implementables condicionales a pre-requisitos operativos o Anexo A (43/44/45/46+47), 0 specs sin diseñar, 0 specs con decisiones bloqueantes pendientes. **Próximos pasos del epic son operativos + Spec 39B**
tipo: documento-epic
afecta: [platform/frontend (capa de visualización sobre el mapa), platform/data (pipelines de datos macro y políticos), 70-Producto/specs (Specs 39-48 + 39B + 41B), 70-Producto/datos-viento (NUEVO directorio del vault)]
depende_de: []
relaciona_con: [Spec 11 (rediseño home/dashboard), Spec 14 (indicadores estructurales), Spec 14A (curaduría 24 indicadores), Spec 14B (pipeline macro existente), Spec 22 (mapa Torres García), Spec 29 (calendario de agentes), Spec 34 (rediseño home r2)]
specs_vinculadas: [39, 39B, 40, 40B, 41, 41B, 42, 43, 44, 45, 46, 47, 48]
prioridad: media
naturaleza: documento vivo — fuente de verdad del epic, se actualiza con cada decisión cerrada o spec mergeada
---

# 03 · Capas analíticas en el mapa principal

## Resumen ejecutivo

Mapa Inestable usa hoy el mapa Torres García como interfaz de navegación: hot-zones por país que abren análisis editoriales. Este epic agrega una **capa de lectura adicional inspirada en mapas meteorológicos** (referencia: Windy). Cuatro variables superpuestas — viento, temperatura, presión, precipitación — visualizadas como sistema dinámico sobre la geografía sudamericana.

El epic habilita **dos lecturas** sobre el mismo objeto:

- **Editorial** — el mapa muestra el sistema político-económico como clima en transformación. Complementa los análisis de los 6 ejes con una capa de datos duros que ancla la lectura conceptual.
- **Risk management** — el mismo mapa, leído con framing de evaluación país, sirve a clientes B2B (consultoras, finanzas, comercio exterior, exportadores) que necesitan entender exposición regional sin leer ensayo.

La metáfora climática es central: no asigna valoración moral a una dirección del viento, solo describe el sistema. Es coherente con el método del proyecto — no es un tablero electoral, es un mapa de procesos estructurales.

**Outcome.** Una persona puede abrir el mapa y entender, sin leer ningún análisis, cómo se está moviendo la región. Un cliente B2B puede usar el mismo objeto para evaluar tendencias estructurales por país.

**Estado actual (2026-05-19).** El epic está en ejecución activa, no en planificación:

- **Spec 39 (arquitectura de capas + layout de `/mapa`)** está implementada en el frontend. Contrato `Layer`, 4 stubs, controlador/leyenda/time-slider/reading-drawer, extensión de `MapaTorresGarcia` con `activeLayer`, polígonos consumidos desde el JSON del vault.
- **Spec 40 (pipeline macro)** está **implementada**. Pipeline extendido con `series_trimestral` / `series_mensual`, indicador `c7-salario-real-mensual`, priority stubs `d1/d2`, CHANGELOG, README con contrato público, JSON en `macro-v1.1.0`. AC2/AC3 (sub-anuales) se poblan cuando haya acceso a red a IMF IFS e ILO.
- **Spec 41 (pipeline político / viento)** está **implementada**. Sistema completo operativo: 10 carpetas por país en `70-Producto/datos-viento/`, archivos piloto AR-W18/W19, pipeline `build_viento.mjs`, JSON compilado en frontend, `lib/viento.ts` con tipos y helpers, skill `coding-viento` en el plugin, Spec 29 actualizada con tareas viernes 16:00 y 19:00.
- **Spec 42 (capa precipitación)** está en **borrador-r2**, lista para handoff a VS Code. Cierre del Anexo A en sesión de Product Design del 2026-05-20: **A.4** combinación tooltip mínimo + drawer completo (subindicadores solo en drawer con sparklines + `invertGood` para inflación/deuda); **B.4** dirección solo en leyenda y tooltip (mapa muestra solo magnitud — restricción A.5 relajada conscientemente); **C.4** glyph nube + gotas en trazo manuscrito Grabado. Patrón cerrado para Specs 43-45, con caveat para Spec 44 viento donde la dirección es la lectura principal y puede necesitar mecanismo visual propio. **Implementación parcial confirmada en frontend (2026-05-21):** `lib/layers.ts` extendido con `LayerSubIndicator` (con `invertGood` y `getSeries`) y `editorialByCountry?`; `lib/layers/precipitacion.ts` implementado con subindicadores y editoriales AR/BR/CL; glyph `precipitacion.svg` sincronizado en `public/mapa/glyphs/`. Pendiente operativo: tokens CSS `--mi-precipitacion-*` en `styles/layers.css` (archivo aún no creado en VS Code).
- **Spec 43 (capa temperatura)** está en **borrador-r2** (2026-05-21). Segunda capa real del epic. Hereda contrato `Layer` ya extendido por Spec 42 y los patrones A.4/B.4 sin tocar. Divergencias locales documentadas: (a) etiqueta del bucket 0 = "Estancado" — confirmado por Tomás como lectura latam donde el congelamiento de salario real es señal activa de tensión, no neutralidad; (b) set de subindicadores `c1-desempleo + c2-informalidad + d3-gini + a1-pbi-pc-ppp` con 3 de 4 `invertGood: true`; (c) dependencia operativa explícita: pipeline `c7-salario-real-mensual` debe estar hidratado contra OIT ILOSTAT (`EAR_4MTH_SEX_ECO_CUR_NB_M`) antes de implementar — pendiente ya agendado en Spec 40 §pendientes. La capa declara cadencia trimestral agregando mensual c7 → trimestral por promedio simple en runtime, variación interanual year-over-year para quitar ruido estacional. **Anexo A cerrado en r2 (2026-05-21)** en sesión de Product Design (Mapa Inestable Design System · spec43/index.html): decisión #17 (glyph SVG `temperatura.svg`) resuelta como **T.1 — termómetro + 3 marcas de escala** sobre 4 candidatos alternativos (T.2 sol, T.3 mercurio, T.4 ondas, T.5 llama). Cuerpo en `<path>` cerrado con curvas Bezier + 3 marcas asimétricas (4/3/4); patrón formal paralelo a `precipitacion.svg`. El glyph no codifica dirección (B.4 heredado). SVG entregado al vault como SSOT; sincronización al frontend pendiente para la sesión VS Code. README de glyphs actualizado, AC15 cerrado del lado del SSOT. T.4 (ondas/sinusoides) queda reservado para Spec 44 (viento). Spec sin decisiones bloqueantes — lista para handoff a VS Code cuando se cumpla el pre-requisito operativo.
- **Spec 45 (capa presión)** está en **borrador-r1** (2026-05-21). **Cuarta y última capa real del epic — cierra el bloque "diseño de capas".** Hereda contrato `Layer` y patrones A.4/B.4/C.4 de Spec 42 r2 con 4 divergencias locales documentadas: (a) escala sobre valor absoluto del compuesto, no variación interanual; (b) indicador principal compuesto "Confianza institucional" = promedio simple de confianza-partidos + congreso + judicial (calculado en runtime en `lib/layers/presion.ts`, no pre-procesado al JSON); (c) `noData` estricto por país-ola sin fallback a ola previa (divergencia vs Spec 44 que usó `getLastVientoBeforeWeek`); (d) etiqueta bucket 0 = "Vacío institucional". 4 subindicadores: los 3 componentes del compuesto + `elecciones-fraudulentas` (eje Desorientación, `invertGood: true`). Cobertura editorial piloto AR/BR/CL idem Spec 42/43. B.4 hereda sin tensión — el bloque "Dirección" de la leyenda se reformula localmente como bloque "Lectura" porque la magnitud absorbe toda la lectura. 14 decisiones técnicas cerradas, 1 decisión visual abierta (#15 glyph SVG `presion.svg`) en Anexo A para sesión de Product Design. Pre-requisito operativo declarado en frontmatter: curar 4 olas históricas LB (2018, 2020, 2021, 2023) además de la 2024 ya hidratada. **Con Spec 45 r1, las 4 capas del epic quedan diseñadas — el epic entra en fase de "ejecución pura"** (operativos + Specs 46/47/39B).
- **Spec 44 (capa viento)** está en **borrador-r2** (2026-05-21). Tercera capa real del epic. Hereda contrato `Layer` y patrones A.4/C.4 de Spec 42 r2; **diverge en B.4** en favor del **glyph orientado sobre el mapa** porque la dirección pro-mercado ↔ pro-estado es la lectura principal de la capa, no auxiliar como en precipitación/temperatura. **Cierra la decisión condicional #3 de Spec 42 r2 y Spec 43 r2** sobre el caveat heredable de B.4 para viento. r1 cerró 17 decisiones (12 heredadas/adaptadas + 5 divergencias locales conscientes: sin subindicadores en r1, 4 buckets en lugar de 5, etiqueta bucket 0 = "Neutro", piloto editorial solo AR, cadencia semanal nativa con `getLastVientoBeforeWeek` como fallback). **Anexo A cerrado en r2** (2026-05-21) en sesión de Product Design (Mapa Inestable Design System · spec44/index.html): decisión #18 acoplada resuelta como **P.1 onda + punta direccional** sobre 4 candidatos alternativos (P.2 flecha + estela, P.3 veleta vertical, P.4 chevron desacoplado, P.5 molinete rotacional) y **V3 asimetría intrínseca + neutro distinto** sobre 4 mecánicas alternativas (V1 variantes separadas, V2 rotación 180° pura, V4 glyph + indicador desacoplado, V5 glyph compuesto continuo). 2 SVGs entregados al vault como SSOT: `viento.svg` (activo canónico, pro-mercado) + `viento-neutro.svg` (variante propia para `rank=0`, no ausencia). Pro-estado se renderea con `transform: scaleX(-1)` aplicado por el componente del mapa a `viento.svg`. El glyph aporta un **tercer eje compositivo** al sistema (stroke abierto horizontal), distinto de los contornos cerrados de precipitación (horizontal) y temperatura (vertical). El vocabulario de ondas/sinusoides queda consumido por esta capa (caveat heredable Spec 42→44 cerrado); Spec 45 (presión) hereda restricción: no reusar ondas — los vocabularios disponibles documentados en README de glyphs (anillos, barras, asterismos, contornos verticales más altos, eje vertical abierto). AC15 cerrado del lado del SSOT; AC16 con mecánica V3 definida — sincronización al frontend y verificación visual se cierran en sesión VS Code. Pre-requisito operativo restante para handoff: cobertura mínima de coding publicado (AR ≥4 semanas, BR/CL ≥2 semanas, resto ≥1 semana).
- **Reframe operativo cerrado:** las capas viven exclusivamente en `/mapa`; el home sigue como mapa navegacional puro con un CTA discreto a `/mapa`. Doble lectura editorial / risk management se materializa dentro de `/mapa`, sin necesidad de dos rutas.
- **Specs hijas postergadas:** Spec 39B (página dedicada de documentación profunda por capa, se diseña al final del epic con las 4 capas implementadas) y Spec 41B (algoritmo híbrido de coding viento, se diseña con 8-12 semanas de coding manual acumulado).

**Lo que entra:**

- Sistema de capas raster/vectoriales sobre el mapa Torres García (toggle, leyenda, time slider).
- Cuatro capas conceptuales (viento, temperatura, presión, precipitación) con fuentes de datos definidas.
- Pipeline de ingestión de datos económicos y políticos (backend + storage + sync).
- Modo lectura editorial + modo lectura risk management (la forma exacta queda abierta — ver Decisión 3).
- Sistema de leyenda y onboarding visual de la metáfora climática.
- Eventualmente, export / snapshot / alertas si va el framing B2B (Spec 48).

**Lo que NO entra:**

- Cambios al modelo de datos editorial (publicaciones, despachos, agendas, agentes diarios siguen igual).
- Generación de análisis editoriales basados en las capas (eso es trabajo humano, no una feature del sistema).
- Predicciones / forecasting (las capas muestran estado y serie temporal, no predicción).
- Visualización subnacional como default (puede entrar como spec posterior para Brasil y Argentina si hay caso).
- Reescritura del mapa Torres García en sí (Spec 22 sigue vigente; este epic se monta encima).

---

## Las cuatro capas

| Capa | Qué representa | Fuente concreta | Cadencia | Estado de pipeline |
|---|---|---|---|---|
| **Viento** | Orientación político-económica: dirección (pro-mercado ↔ pro-estado) e intensidad (velocidad del cambio) | Coding editorial manual semanal (Spec 41 implementada). JSON compilado `viento-v1.0.0` con `rank ∈ {-3..+3}`, `direccion`, `intensidad`, `justificativo`, `eventos[]`. Algoritmo híbrido pospuesto a Spec 41B (espera 8-12 semanas de coding acumulado) | Semanal | Pipeline **implementado** (Spec 41); capa en **Spec 44 borrador-r2** — 19 decisiones cerradas en total (17 en r1 + 2 visuales en r2: #18.a P.1 onda + punta, #18.b V3 asimetría + neutro distinto). 2 SVGs entregados al vault. **B.4 diverge** en favor de glyph orientado sobre el mapa; vocabulario de ondas consumido por esta capa. Pre-requisito operativo: cobertura mínima de coding publicado |
| **Temperatura** | Condiciones materiales de la población. Salario real como base (variación interanual del índice OIT base 2021=100), 4 subindicadores enriquecen la lectura (desempleo, informalidad, Gini, PBI per cápita PPP) | OIT ILOSTAT (`EAR_4MTH_SEX_ECO_CUR_NB_M`) para el principal; pipeline `macro-v1.1.0` para los 4 subindicadores (cobertura 9-10/10 según subindicador, VE faltante en `d3-gini` y `a1-pbi-pc-ppp`) | Trimestral (agregando mensual original) con fallback soft a noData cuando c7 no hidratado | Pipeline **implementado** (Spec 40) excepto c7 que sigue stub; capa en **Spec 43 borrador-r2** — 17 decisiones cerradas (16 técnicas + 1 visual del Anexo A: glyph T.1 termómetro + 3 marcas), AC15 cerrado del lado del SSOT, pre-requisito operativo: hidratar c7 contra OIT |
| **Presión** | Densidad de mediaciones institucionales: confianza en partidos, congreso y poder judicial (compuesto promedio simple) + suelo epistémico de la democracia (elecciones-fraudulentas como subindicador externo) | Latinobarómetro multi-ola, indicadores curados en Spec 12A. Pipeline actual: ola 2024 hidratada (`lb2024-v20250817`); pre-req: curar 4 olas históricas (2018, 2020 modo crisis COVID, 2021, 2023). Lib `latinobarometro.ts` requiere extensión para multi-ola (`getAvailableWaves`, `getWaveByYear`, `getCountryDataByWave`) | Anual nativa, 5 olas. `noData` estricto sin fallback (divergencia vs Spec 44) | Ola 2024 implementada; capa en **Spec 45 borrador-r1** — 14 decisiones técnicas cerradas, 1 decisión visual abierta (#15 glyph), pre-requisito operativo: curar 4 olas históricas LB |
| **Precipitación** | Estado de la economía: crecimiento vs. estancamiento, inflación | Banco Mundial (`a2-crecimiento-pbi.series_trimestral` con fallback anual) + 4 subindicadores del pipeline macro-v1.1.0 (`a6-inflacion-ipc`, `a5-inversion-pbi`, `b6-deuda-pbi`, `a4-productividad-laboral`) | Trimestral con fallback anual | Pipeline **implementado** (Spec 40); capa en **Spec 42 borrador-r1** — 12 decisiones técnicas cerradas, 3 decisiones visuales en Product Design |

La combinación de las cuatro capas describe el **clima país**: un país puede tener viento pro-mercado fuerte + temperatura baja (salarios cayendo) + presión baja (instituciones erosionadas) + precipitación alta (estancamiento). Esa lectura compuesta es lo que ningún análisis textual puede dar de un vistazo.

---

## Decisiones cerradas

| Fecha | Decisión | Rationale | Qué desbloquea |
|---|---|---|---|
| 2026-05-18 | La metáfora climática se conserva, pero las categorías se reescriben para alinearse al marco del proyecto | "Izquierda/derecha" importa el marco que el proyecto cuestiona (desrepresentación, deculturación deforman esas categorías). El clima como sistema sin valoración moral encaja con el método | Decide el lenguaje de la capa viento y la lógica conceptual de las cuatro |
| 2026-05-18 | La capa **viento** se codifica como "pro-mercado ↔ pro-estado", no izquierda/derecha | Más operativo y medible que el autoetiquetado; sirve mejor para risk management (Milei es de derecha pero hace ortodoxia FMI; Lula es de izquierda pero sostiene macro ortodoxa) | Hace escribible la Spec 44 (capa viento) |
| 2026-05-18 | Doble audiencia: editorial + risk management sobre el mismo mapa | Mismo objeto sirve a dos lecturas si las capas están bien diseñadas. No hay producto B2B separado; hay un mismo producto leído de dos maneras | Define el alcance del modo risk management (Spec 48) y abre Decisión 3 |
| 2026-05-18 | **Temperatura** = salario real (base), con opción de combinar con ratio mediano/promedio | Datos disponibles en INDEC/IBGE/INE; el ratio mediano/promedio agrega lectura de desigualdad casi gratis | Hace escribible la Spec 43 |
| 2026-05-18 | **Presión** = índices de Latinobarómetro | Datos ya curados en Specs 12A y 14A; aprovecha el pipeline existente | Hace escribible la Spec 45 (en parte) |
| 2026-05-18 | **Precipitación** = crecimiento económico | La más directa de implementar técnicamente; buena para validar la arquitectura punta a punta | Hace de Spec 42 la primera capa a construir |
| 2026-05-18 (r2) | **Capa viento: codificación híbrida.** Algoritmo provee la base; el autor puede hacer override editorial cuando la lectura algorítmica no captura un movimiento importante | Más robusta y defendible que algoritmo puro u editorial puro; el override editorial vive en el vault siguiendo el patrón borrador → promote de Spec 29 | Hace escribibles Specs 41 y 44, define el modelo de datos de la capa |
| 2026-05-18 (r2) | **Modelo de tiempo: por capa, no uniforme.** Cada capa se muestra siempre con su última lectura disponible al momento que apunta el slider, con la fecha explícita en la leyenda | Honesta sobre las distintas cadencias (viento semanal, temperatura mensual, precipitación trimestral, presión anual). Evita el error de leer la presión de hoy como si fuera la presión de ayer | Hace escribible Spec 39 (UI del slider) y el modelo de datos de Spec 40 |
| 2026-05-18 (r2) | **Visualización: una UI toggleable estilo Windy en v1.** Todas las capas disponibles desde el mismo controlador; el usuario elige qué activar | Empezar simple, ver cómo lo usa la gente antes de diferenciar. Modos editorial/risk como capa de personalización en una iteración posterior | Hace escribible Spec 39 (controlador de capas), simplifica navegación del sitio, posterga decisión de modelo de cuentas B2B |
| 2026-05-18 (r2) | **Profundidad histórica: 5 años en v1.** Time slider cubre desde 2021 | Cubre la cobertura del proyecto sin volver el slider inmanejable; casi todas las fuentes tienen esa profundidad sin pagar | Acota el costo de Spec 40 (storage e ingestión) y la performance del slider |
| 2026-05-18 (r2) | **Granularidad geográfica nivel país en v1.** Subnacional (Brasil, Argentina) queda como spec posterior si hay caso | Reduce el alcance de la primera iteración; subnacional cuadruplica el costo de ingestión y de UI. Las fuentes usadas serán todas de licencia libre o atribución manejable | Cierra Specs 40 y 41 a alcance manejable; deja el upgrade subnacional como decisión futura |

---

## Decisiones abiertas

Las 5 decisiones estratégicas del epic quedaron cerradas en la r2 del 2026-05-18 — ver tabla arriba. No quedan decisiones abiertas a nivel epic.

Las próximas decisiones aparecerán a nivel táctico cuando arranque la implementación de cada spec (qué endpoint exacto de cada fuente, qué algoritmo específico para el scoring de la capa viento, qué librería de rendering de partículas, etc.). Esas decisiones se cierran dentro de cada spec, no acá.

---

## Specs vinculadas

Ordenadas por dependencia. La numeración arranca en 39 (38 ya está ocupada).

| Spec | Título | Depende de | Estado |
|---|---|---|---|
| 39 | Arquitectura de capas en el mapa | EPIC 03 (decisiones 2, 3, 4) | **borrador-r2** — decisiones cerradas, lista para handoff a VS Code |
| 39B | Página dedicada de documentación por capa | Spec 39 + 4 capas implementadas | placeholder — se diseña al final del epic, con las 4 capas en el aire (no post-Spec 42 como se pensó originalmente) |
| 40 | Pipeline de datos macroeconómicos | EPIC 03 (decisión 5), Spec 14B existente | **implementada** — pipeline extendido con sub-anuales, `c7-salario-real-mensual`, priority stubs, skill staging, Spec 29 actualizada. AC2/AC3 requieren red para datos finales |
| 40B | Fuentes nacionales para c7-salario-real-mensual | Spec 40, Spec 43 (consumidor) | **borrador-r1** — sub-spec condicional. Política B (nacional siempre, OIT fallback), 5 países nacionales (AR INDEC, BR IBGE, CL INE, UY INE, PE INEI), 5 OIT-only. Schema bump a `macro-v1.2.0` con `methodology_by_country?` opcional. Pre-requisito: correr OIT primero y solo implementar si cobertura insuficiente para AR/BR/CL |
| 41 | Pipeline de datos políticos (capa viento) | EPIC 03 (decisiones 1, 5) | **implementada** — vault + pipeline Node.js + JSON frontend + skill en plugin |
| 41B | Algoritmo híbrido de coding para capa viento | Spec 41 + 8-12 semanas de coding manual acumulado | placeholder — se diseña cuando haya material editorial de referencia |
| 42 | Capa precipitación: crecimiento económico | Spec 39, 40 | **borrador-r2 + implementada parcial** — 15 decisiones cerradas. Frontend: `lib/layers.ts` extendido + `lib/layers/precipitacion.ts` implementado + glyph sincronizado. Pendiente: tokens CSS `--mi-precipitacion-*` en `styles/layers.css` |
| 43 | Capa temperatura: salario real / desigualdad | Spec 39, 40, 42 | **borrador-r2** — 17 decisiones cerradas (16 técnicas + 1 visual del Anexo A: glyph T.1 = termómetro + 3 marcas). AC15 cerrado del lado del SSOT (sync al frontend en sesión VS Code). Pre-requisito operativo pendiente: pipeline `c7-salario-real-mensual` hidratado contra OIT (de Spec 40, no de esta spec) |
| 44 | Capa viento: orientación pro-mercado / pro-estado | Spec 39, 41 | **borrador-r2** — 19 decisiones cerradas en total (17 en r1 + 2 visuales en r2: #18.a P.1 onda + punta, #18.b V3 asimetría intrínseca + neutro distinto). 2 SVGs entregados al vault (`viento.svg` + `viento-neutro.svg`). AC15 cerrado del lado del SSOT. Pre-requisito operativo restante: cobertura mínima de coding publicado |
| 45 | Capa presión: densidad institucional desde Latinobarómetro | Spec 39, 12A, 14A | **borrador-r1** — 14 decisiones técnicas cerradas, 1 decisión visual abierta (#15 glyph SVG `presion.svg`) en Anexo A. 4 divergencias locales documentadas. Pre-requisito operativo: curar 4 olas históricas LB (2018, 2020, 2021, 2023) además de la 2024 ya hidratada |
| 46 | Sistema de leyenda y onboarding visual | Spec 39 + al menos una capa publicada | **borrador-r1** — 15 decisiones técnicas cerradas, 1 decisión visual abierta (#16 header glyph compuesto del overlay de pantalla 1) en Anexo A. Onboarding como eje central + leyenda r2 con badge de calidad + microcopy + accordion. Extiende contrato `Layer` con `legendMicrocopy` y `shortIntro`. Lista para handoff a VS Code con placeholder visual mientras Anexo A cierra |
| 47 | Tooltip e interacción multi-capa | Spec 39 + 2+ capas publicadas | **borrador-r1** — 16 decisiones técnicas cerradas, 1 decisión visual abierta (#17 diferenciación del chip cruzado) en Anexo A. Single-select se confirma como ancla del epic (cierra la decisión condicional #4 sin romperla); snapshot cruzado, panel pineado y bloque "Otras capas" en drawer. Comparación país×país fuera de scope r1. Extiende contrato `Layer` con `formatCrossLayerChip?` y agrega helper `getCrossLayerSnapshot` reutilizable. Lista para handoff a VS Code con layout D.1 default mientras Anexo A cierra |
| 48 | Modo risk management (export, alertas, snapshot) | Spec 39 + decisión 3 cerrada en favor de (b), o como adición a (a) | condicional |

Estimación gruesa: ~10 specs, 2-3 sprints de trabajo bien hecho, asumiendo que las cinco decisiones estratégicas estén cerradas antes de arrancar.

---

## Próximos pasos

1. ~~Cerrar las 5 decisiones estratégicas.~~ **Hecho en r2 del epic (2026-05-18).**
2. ~~Diseñar Spec 39 (arquitectura).~~ **Hecho 2026-05-18; implementada en frontend.**
3. ~~Diseñar Specs 40 y 41 (pipelines).~~ **Hechas 2026-05-18; cerradas tácticas en r2 el 2026-05-19; listas para handoff.**

**Próximos en orden recomendado:**

4. ~~**Mandar Spec 40 a VS Code para implementar.**~~ **Hecho 2026-05-19** — pipeline implementado, JSON `macro-v1.1.0` en frontend. Desbloquea Specs 42 y 43.
   - **Pendiente (Cowork):** crear scheduled task `pipeline-macro-refresh` viernes 18:00 ART + importar `70-Producto/skills/pipeline-macro-refresh/SKILL.md` al plugin.
   - **Pendiente (laptop con red):** correr `py build_indicators_macro.py` con acceso a internet para poblar `a2-crecimiento-pbi.series_trimestral` (IMF IFS, AC2) y `c7-salario-real-mensual.series_mensual` (ILO, AC3).
5. ~~**Mandar Spec 41 a VS Code para implementar.**~~ **Hecho 2026-05-20** — sistema de coding viento completo. Desbloquea Spec 44.
   - **Pendiente (Cowork):** crear scheduled tasks en Cowork (viernes 16:00 `coding-viento-recordatorio` + viernes 19:00 `build-viento`).
6. ~~**Diseñar Spec 42 (capa precipitación) en Cowork.**~~ **Hecho 2026-05-20.** r1 cerró 12 decisiones técnicas; r2 cerró las 3 decisiones visuales del Anexo A en sesión de Product Design (A.4 combinación tooltip + drawer, B.4 dirección solo en leyenda y tooltip, C.4 glyph nube + gotas). 15 decisiones cerradas. Resuelve la pregunta abierta #4 del epic en sus dos dimensiones (técnica y visual). **Lista para handoff a VS Code.**
7. ~~**Diseñar Spec 43 (capa temperatura) en Cowork.**~~ **Hecho 2026-05-21.** r1 cerró 16 decisiones técnicas; r2 cerró la única decisión visual del Anexo A en sesión de Product Design (decisión #17: glyph `temperatura.svg` = termómetro con bulbo + 3 marcas de escala, formato C.4 heredado de Spec 42 r2). 17 decisiones cerradas en total, AC15 cerrado, SVG entregado al vault como SSOT. Pre-requisito operativo declarado: hidratar c7 contra OIT (pendiente de Spec 40) antes de implementar en VS Code.
   - **2026-05-21 — Spec 40B creada como sub-spec condicional** para resolver el hueco de cobertura OIT en AR/BR/CL (los 3 pilotos editoriales de Spec 43). Política B (nacional siempre, OIT fallback), 5 países nacionales (AR INDEC, BR IBGE, CL INE, UY INE, PE INEI). r1 lista para handoff a VS Code SI la corrida OIT confirma cobertura insuficiente; si OIT alcanza, queda en archivo.
8. ~~**Diseñar Spec 44 (capa viento) en Cowork.**~~ **r1 hecho 2026-05-21; r2 hecho 2026-05-21.** Tercera capa real del epic. r1 cerró 17 decisiones (12 heredadas/adaptadas + 5 divergencias locales conscientes); r2 cerró la decisión #18 acoplada en sesión de Product Design (Mapa Inestable Design System · spec44/index.html): #18.a P.1 onda + punta direccional + #18.b V3 asimetría intrínseca + neutro distinto. 19 decisiones cerradas en total. 2 SVGs entregados al vault. AC15 cerrado del lado del SSOT, AC16 con mecánica V3 definida. **Cierra la decisión condicional #3 de Spec 42 r2 y Spec 43 r2 sobre B.4 para viento** (Spec 44 diverge en favor de glyph orientado sobre el mapa). Sistema de glyphs consolidado en 3 ejes compositivos distintos (horizontal cerrado, vertical cerrado, horizontal abierto). Vocabulario de ondas/sinusoides consumido por esta capa. Pre-requisito operativo restante: cobertura mínima de coding publicado (AR ≥4 semanas, BR/CL ≥2 semanas, resto ≥1 semana) antes del handoff a VS Code. **Próximo paso:** ritmo semanal del editor para cubrir el pre-requisito + handoff a VS Code (1-2 días estimados).
9. ~~**Diseñar Spec 45 (capa presión) en Cowork.**~~ **Hecho 2026-05-21.** Cuarta y última capa real del epic. r1 cerró 14 decisiones técnicas (combinación de heredadas de Spec 42 r2 + decisiones específicas con Tomás): compuesto "Confianza institucional" como principal, 4 subindicadores (3 del cluster + elecciones-fraudulentas), escala secuencial sobre valor absoluto del compuesto, `noData` estricto sin fallback, etiqueta bucket 0 = "Vacío institucional", piloto editorial AR/BR/CL idem Spec 42/43. **B.4 hereda sin tensión** porque la magnitud absorbe toda la lectura (sin dirección separable) — el bloque "Dirección" de la leyenda se reformula localmente como bloque "Lectura". 1 decisión visual abierta (#15 glyph) en Anexo A para sesión de Product Design. Pre-requisito operativo declarado: curar 4 olas históricas LB (2018, 2020 modo crisis COVID, 2021, 2023). **Con Spec 45 r1 cerrada, las 4 capas del epic quedan diseñadas** — el epic entra en fase de "ejecución pura" (operativos + Specs 46/47/39B). **Próximo paso:** sesión de Product Design para cerrar Anexo A (decisión #15) → r2 → handoff a VS Code una vez cumplido el pre-requisito operativo.
10. ~~**Diseñar Specs 46 y 47** (leyenda + onboarding, tooltip multi-capa) cuando haya al menos 2 capas implementadas para diseñar sobre uso real.~~ **Hecho 2026-05-21.** Las dos specs se diseñaron en una sola sesión de Cowork. Spec 46 cerró 15 decisiones técnicas con onboarding como eje central + refinamientos a la leyenda; Spec 47 cerró 16 decisiones técnicas confirmando single-select como ancla del epic y resolviendo la necesidad multi-capa con tooltip cruzado (sin multi-activación). Ambas dejan 1 decisión visual cada una para Product Design (#16 header glyph compuesto, #17 diferenciación del chip cruzado). Tres patrones nuevos consolidados: (a) onboarding versionado en `localStorage` reactivable cuando el sistema cambie sustantivamente, (b) extensión del contrato Layer con `legendMicrocopy` + `shortIntro` + `formatCrossLayerChip?` opcionales, (c) primitivo reutilizable `getCrossLayerSnapshot` que Spec 48 puede consumir. **Próximo paso:** sesiones de Product Design para los dos Anexos A (paralelizable con cobertura operativa de Specs 43/44/45) → handoff a VS Code.
11. **Diseñar Spec 39B (página dedicada de documentación profunda por capa)** al final del epic, con las 4 capas implementadas.
12. **Decidir Spec 48 (modo risk management)** según resolución de la pregunta abierta #1 sobre tier B2B pago.
13. **Diseñar Spec 41B (algoritmo híbrido de coding viento)** después de 8-12 semanas de coding manual acumulado.

**Recordatorio de flujo de trabajo.** El diseño de cada spec (escritura del documento en `70-Producto/specs/`) se hace en sesiones de **Claude Cowork**. La implementación de cada spec (código en `platform/`) se hace en **VS Code con Claude Code**. El handoff entre ambos es el archivo de spec mergeado, listo para ser ejecutado. Ver sección "Flujo de trabajo" en `CLAUDE.md`.

**Velocidad esperada (basada en lo recorrido):** una sesión de Cowork cierra el diseño de una spec en 30-60 minutos cuando las decisiones estratégicas del epic ya están cerradas. Una sesión de VS Code implementa una spec en 1-3 días. A este ritmo, el epic completo (4 capas + tooling + página de docs) cierra en 4-6 semanas de trabajo distribuido.

---

## Histórico

| Fecha | Cambio | Razón |
|---|---|---|
| 2026-05-18 | Creación del documento. Origen: conversación sobre cómo aplicar la metáfora de mapas meteorológicos (Windy) al mapa Torres García. Inaugura la carpeta `70-Producto/epics/`, primer epic formal del proyecto | Spec 29 funcionó como meta-spec del sistema de agentes; surgió la necesidad de un patrón equivalente para features grandes del frontend |
| 2026-05-18 | Renumeración: las specs del epic arrancan en 39 (38 ya estaba tomada por "columna home borradores diarios") | Conflict de numeración detectado al revisar el directorio de specs |
| 2026-05-18 (r2) | Cerradas las 5 decisiones estratégicas siguiendo las recomendaciones preliminares: capa viento híbrida, modelo de tiempo por capa, UI única toggleable en v1, profundidad histórica 5 años, granularidad nivel país en v1. Estado del epic pasa de `borrador-r1` a `decisiones-cerradas` | Decisión explícita del autor; epic queda desbloqueado para arrancar Spec 39 |
| 2026-05-18 (r2) | Agregado al epic recordatorio del flujo de trabajo Cowork/VS Code en sección "Próximos pasos" | Garantizar que cualquier sesión que retome este epic sepa dónde se hace cada cosa |
| 2026-05-18 | Spec 39 mergeada en borrador-r2: arquitectura + layout de `/mapa` + reframe (home preview / `/mapa` analítico). Cerradas 22 decisiones (11 estratégicas + 11 tácticas). SSOT de polígonos de hot-zones movido al vault. Render con Gaussian blur para difuminar fronteras. Spec 39B creada como placeholder | Avance del epic: la spec madre está lista para handoff a VS Code. Spec 39B captura el principio "no esconder los trucos del mago" sin inflar Spec 39 |
| 2026-05-18 | Spec 40 mergeada en borrador-r1: formalización del pipeline macro existente (Spec 14B) + extensión a cadencias sub-anuales (trimestral, mensual) para alimentar capas precipitación y temperatura. Skill `pipeline-macro-refresh` + scheduled task semanal. Reposicionamiento de Spec 39B: se diseña al final del epic, no post-Spec 42 (decisión refinada por Tomás — para que el patrón documental sea consistente con las 4 capas implementadas) | Spec 40 desbloquea Specs 42 y 43. El pipeline existente reduce mucho el trabajo: no es construir desde cero, es formalizar contrato + agregar 2 indicadores nuevos |
| 2026-05-18 | Spec 41 mergeada en borrador-r1: MVP 100% manual editorial para alimentar la capa viento. Sistema de coding semanal con escala -3 a +3, archivo .md por país-semana en `70-Producto/datos-viento/<slug>/`, skill `coding-viento` que asiste, pipeline Node.js que compila JSON, scheduled tasks. Spec 41B creada como placeholder del algoritmo híbrido (se diseña cuando haya 8-12 semanas de coding manual acumulado). Spec 39 verificada como implementada en el frontend en sesión paralela de VS Code | Diseñar el algoritmo del coding viento sin material editorial sería inventar reglas. El coding manual es el input necesario para entender qué patrones existen antes de automatizar |
| 2026-05-19 | Spec 40 bumpeada a borrador-r2: cerradas 5 de las 6 decisiones tácticas abiertas (script Python en sandbox de Cowork, cobertura 6-8 países en r1, auto-promote con umbral 5%, `frozen_since?` para indicadores con fuente discontinuada, ratio mediano/promedio confirmado para cuando Spec 43 lo pida). Queda abierta sólo la #2 (historial de revisiones retroactivas) por decisión explícita: se decide tras 4-6 semanas de operación. Lista para handoff a VS Code sin decisiones bloqueantes | Tomás cerró las tácticas tras leer r1. La spec queda implementable sin volver a Cowork |
| 2026-05-19 | Spec 41 bumpeada a borrador-r2: cerradas las 6 decisiones tácticas abiertas (Tomás único codificador, sub-eventos sin rank propio en r2, paleta CSS cae en Spec 44, trigger on-publish vía skill, cobertura ≥1 país válida, histórico de revisiones pospuesto). Lista para handoff a VS Code sin decisiones bloqueantes. EPIC 03 ya tiene 3 specs listas (39 implementada + 40 r2 + 41 r2) y 2 placeholders (39B, 41B) | Tomás revisó r1 y cerró todas las tácticas. Próximo paso natural: diseñar Spec 42 (capa precipitación) que ya puede asumir Spec 40 implementada |
| 2026-05-19 (r3 del epic) | Actualización integral del documento del epic: estado cambia de `decisiones-cerradas` a `en-ejecucion`. Agregado párrafo de "Estado actual" al resumen ejecutivo. Refinada la tabla de las 4 capas con fuentes concretas + columna de estado de pipeline. Reescrita la sección "Próximos pasos" según el estado real (los 3 primeros pasos ya hechos). Resueltas las preguntas abiertas #2 (publicación de a una, no en bloque) y #4 (manejo de fuentes que no actualizan — resuelto en Spec 40 r2). Agregada pregunta abierta nueva #4 sobre subcapas / sub-indicadores planteada por Tomás. Specs_vinculadas extendidas con 39B y 41B en el frontmatter | El epic dejó de ser "planificación cerrada" para pasar a "ejecución activa" — el doc tenía que reflejar ese cambio para que cualquier sesión futura entienda dónde está parado |
| 2026-05-19 | Spec 40 implementada en sesión de Claude Code (VS Code). Estado en tabla de specs vinculadas pasa de `borrador-r2` a `implementada`. Próximo paso 4 tachado. Tabla de capas y resumen ejecutivo actualizados. Pipeline `macro-v1.1.0` activo en frontend | Primera spec del pipeline de EPIC 03 implementada; desbloquea diseño de Spec 42 (capa precipitación) |
| 2026-05-20 | Spec 41 implementada en sesión de Claude Code (VS Code): `70-Producto/datos-viento/` (10 carpetas + `_compilado/` + 2 archivos piloto AR), `platform/data/coding-viento/build_viento.mjs` (pipeline Node.js), `platform/frontend/src/data/coding-viento/viento.json` + `platform/frontend/src/lib/viento.ts` (tipos + helpers), skill `coding-viento` en el plugin ZIP, Spec 29 actualizada con 2 nuevas entradas del calendario. Pendiente Cowork: crear los 2 scheduled tasks | Segunda spec de pipeline de EPIC 03 implementada; desbloquea Spec 44 (capa viento visual) |
| 2026-05-20 (r6 del epic) | Spec 42 bumpeada a borrador-r2: cerradas las 3 decisiones visuales del Anexo A en sesión de Product Design (Mapa Inestable Design System · spec42/index.html). **A.4** combinación tooltip mínimo + drawer completo — el tooltip lleva valor PBI con signo y label de dirección; el drawer lleva bloque "Lectura" + bloque "Subindicadores" con 4 sparklines (con `invertGood` para inflación/deuda); en mobile el tap abre directamente el drawer. **B.4** dirección solo en leyenda y tooltip — mapa muestra solo magnitud, la restricción A.5 del Anexo A se relaja conscientemente (las otras restricciones tuvieron más peso editorial). **C.4** glyph nube + gotas en trazo manuscrito Grabado; versión "seca" para recesión se entrega pero no se referencia. AC15 reescrito; AC11 y AC14 cerrados. La spec queda lista para handoff a VS Code sin decisiones bloqueantes | Tomás priorizó "mapa limpio + lectura interpretativa en componentes laterales" sobre "diagnóstico de un vistazo" — coherente con el ADN editorial del proyecto. El patrón cerrado en r2 se ofrece como ancla para Specs 43-45, con caveat explícito para Spec 44 (viento) donde la dirección es la lectura principal y puede necesitar mecanismo visual propio |
| 2026-05-20 (r4 del epic) | Spec 42 creada en borrador-r1. Primera capa analítica real del epic. Decisiones técnicas cerradas: escala secuencial intensidad con 5 buckets de magnitud (signo separado), paleta terracota 5 pasos coherente con dirección Grabado, los 4 subindicadores del pipeline macro-v1.1.0 ligados a la capa (inflación, inversión, deuda, productividad), cobertura editorial piloto ARG/BRA/CHI con cobertura técnica 10/10, contrato técnico de subindicadores (propuesta de extensión `subIndicators?: LayerSubIndicator[]` al contrato Layer en r3 de Spec 39). Decisiones visuales abiertas (UI subindicadores, distinción crecimiento/recesión, glyph SVG) con brief autocontenido en Anexo A para sesión de Product Design separada | Spec 42 es la primera capa real y por eso inaugura tres patrones que Specs 43-45 heredan: subindicadores, cobertura editorial piloto, escala secuencial con dirección. Separar la decisión técnica de la decisión visual permite paralelizar implementación (VS Code) con diseño visual (Product Design) — el contrato técnico de subindicadores está cerrado independientemente del aspecto final |
| 2026-05-21 (r7 del epic) | Spec 42 verificada como **implementada parcialmente en frontend** durante sesión de pre-flight de Spec 43: `lib/layers.ts` ya extiende el contrato con `LayerSubIndicator` (con `invertGood` y `getSeries(slug)` que devuelve hasta 8 puntos para sparkline) y `editorialByCountry?: Record<string, string>`; `lib/layers/precipitacion.ts` implementa los 4 subindicadores anuales y los textos editoriales inline para AR/BR/CL; `public/mapa/glyphs/precipitacion.svg` sincronizado desde el vault. Pendiente pequeño: tokens CSS `--mi-precipitacion-*` en `styles/layers.css` (archivo aún no creado por VS Code). Actualizada tabla de specs vinculadas: estado de Spec 42 ahora "borrador-r2 + implementada parcial" | Tracking honesto del estado del frontend para no asumir que la implementación de Spec 42 está pendiente cuando ya está mayormente hecha. Permite que Spec 43 herede sobre un contrato `Layer` real, no propuesto |
| 2026-05-21 (r7 del epic) | Spec 43 creada en borrador-r1. Segunda capa real del epic. Hereda contrato `Layer` extendido por Spec 42 y los patrones visuales A.4/B.4 intactos. r1 cierra 16 decisiones técnicas. Tres divergencias locales documentadas: (a) etiqueta del bucket 0 = "Estancado" — Tomás identificó que en Latam el congelamiento del salario real es señal activa de tensión, no neutralidad; (b) set de subindicadores `c1-desempleo + c2-informalidad + d3-gini + a1-pbi-pc-ppp` con 3 de 4 `invertGood: true` (dominio bienestar laboral invierte más que dominio macro); (c) dependencia operativa explícita del pipeline `c7-salario-real-mensual` hidratado contra OIT — pendiente ya agendado en Spec 40 §pendientes, sin spec nueva. Cadencia trimestral declarada agregando mensual c7 a trimestral por promedio simple en runtime; variación interanual year-over-year para quitar ruido estacional. Cobertura editorial piloto AR/BR/CL idem Spec 42 por consistencia (vocabulario aprendido del lector). Anexo A reducido a una sola decisión visual (glyph `temperatura.svg`) — A.4 y B.4 se heredan intactos | Spec 43 es la primera spec en heredar el patrón cerrado por Spec 42 r2. Las divergencias locales son chicas y bien razonadas, lo que confirma que el patrón Spec 42 es genuinamente replicable. La spec resuelve la decisión 4 del epic (temperatura = salario real con desigualdad como complemento) con set de subindicadores cerrado |
| 2026-05-21 (r8 del epic) | Spec 40B creada en borrador-r1 — sub-spec condicional de Spec 40 enfocada exclusivamente en agregar fuentes nacionales para `c7-salario-real-mensual` (Argentina INDEC, Brasil IBGE, Chile INE, Uruguay INE, Perú INEI). Política de merge B cerrada: nacional siempre, OIT como fallback. Schema extendido con `methodology_by_country?: Record<string, string>` opcional (no breaking, bump a `macro-v1.2.0`). 12 decisiones técnicas cerradas en r1, sin decisiones bloqueantes. Pre-requisito operativo declarado en el frontmatter: correr la corrida OIT del pipeline primero y solo implementar Spec 40B si la cobertura resultante es insuficiente para los 3 países piloto de Spec 43 (AR/BR/CL) | Las propias notas del indicador c7 (`build_indicators_macro.py` y `sources.py`) reconocen que OIT puede no tener buena cobertura para AR y BR, y recomiendan INDEC RIPTE e IBGE PNAD como fuentes nacionales. Spec 40B materializa esa deuda con un alcance acotado (un indicador, 5 países nacionales) en lugar de reabrir Spec 40 entera. La condicionalidad evita trabajo si la corrida OIT termina alcanzando |
| 2026-05-21 (r8 del epic) | Spec 43 bumpea a borrador-r2 al cerrar la decisión #17 del Anexo A (diseño del glyph SVG `temperatura.svg`) en sesión de Product Design (Mapa Inestable Design System · spec43/index.html). Elegida la opción T.1 — termómetro + 3 marcas — sobre 4 candidatos alternativos (T.2 sol + rayos, T.3 mercurio prominente, T.4 ondas/sinusoides, T.5 llama anidada + chispas). El SVG entrega cuerpo (tubo + bulbo) en un único `<path>` cerrado con curvas Bezier + 3 marcas horizontales asimétricas como secundarios, paralelo formal a `precipitacion.svg`. SVG entregado al vault como SSOT; sincronización al frontend (`platform/frontend/public/mapa/glyphs/temperatura.svg`) pendiente para la sesión VS Code. README de glyphs actualizado, AC15 reescrito y cerrado del lado del SSOT. El epic queda con 1 spec implementable (43, condicional al pre-requisito operativo de c7) y 2 specs todavía en diseño (44 viento, 45 presión). **La elección de T.1 establece un caveat útil para Spec 44**: el vocabulario de ondas/sinusoides (T.4 descartado) queda libre para el glyph viento, y el README de glyphs ya lo documenta como reserva del sistema. El comparador exploratorio que armó Cowork previo a la sesión de Product Design queda como artefacto en `outputs/spec43-glyph-temperatura-comparador.html` — la decisión oficial cerró en la sesión separada de Mapa Inestable Design System | El Anexo A de Spec 43 era chico por diseño — A.4 y B.4 ya estaban heredados de Spec 42 r2, así que la sesión de Product Design solo tuvo que cerrar el glyph. El resultado replica con mínima fricción el patrón "elemento principal + secundarios" cerrado en C.4: precipitación = nube + 3 gotas, temperatura = termómetro + 3 marcas. La economía visual del sistema queda probada como replicable para las dos capas pendientes (viento, presión). La spec 43 ya no bloquea ningún diseño |
| 2026-05-21 (r9 del epic) | Spec 44 (capa viento) creada en borrador-r1. Tercera capa real del epic. Hereda contrato `Layer` (ya extendido por Spec 42 con `subIndicators?` y `editorialByCountry?`) y patrones A.4 (tooltip + drawer) y C.4 (sistema de glyphs "principal + secundarios") sin tocar. **Diverge en B.4 en favor del glyph orientado sobre el mapa** — el color del polígono codifica solo magnitud (escala secuencial terracota con sesgo ocre profundo, 4 buckets); cada hot-zone con dato lleva encima un glyph chico orientado que apunta a izquierda o derecha según la dirección del coding semanal (pro-estado ↔ pro-mercado). **Cierra la decisión condicional #3 de Spec 42 r2 y Spec 43 r2** sobre el caveat heredable de B.4 para viento: la decisión 15 de Spec 44 r1 documenta el cierre con razón explícita (en viento la dirección es la lectura principal, no auxiliar; B.4 puro perdería legibilidad pasiva; paleta diverging rompería coherencia con Spec 42/43). r1 cierra 17 decisiones (12 heredadas/adaptadas + 5 divergencias locales: sin subindicadores en r1, 4 buckets en lugar de 5, etiqueta bucket 0 = "Neutro", piloto editorial solo AR, cadencia semanal con `getLastVientoBeforeWeek` como fallback). 1 decisión abierta (#18) en Anexo A con sub-decisiones acopladas: 18.a (diseño del glyph SVG `viento.svg`) + 18.b (mecánica visual de orientación — variantes izq/der, rotación, asimetría, glyph + indicador, etc.). Pre-requisito operativo declarado en frontmatter: cobertura mínima de coding publicado (AR ≥4 semanas, BR/CL ≥2 semanas, resto ≥1 semana) antes del handoff a VS Code. Decisión condicional para r3 documentada: bloque "Noticias por sector" como subindicadores cuando Spec 41B esté en producción (8-12 semanas de coding acumulado) | Tercera capa real del epic. El método de "patrón heredado + divergencias locales documentadas" que Spec 43 inauguró escala bien — Spec 44 documenta 5 divergencias (vs las 3 de Spec 43), todas con razón explícita. La divergencia mayor (B.4) cierra un caveat que venía abierto desde Spec 42 r2 — coherencia narrativa del epic. La spec queda lista para sesión de Product Design (Anexo A) → r2 → handoff a VS Code una vez cumplido el pre-requisito operativo |
| 2026-05-21 (r11 del epic) | Spec 45 (capa presión) creada en borrador-r1. **Cuarta y última capa real del epic — cierra el bloque "diseño de las 4 capas"**. Hereda contrato `Layer` y patrones A.4/B.4/C.4 de Spec 42 r2 con 4 divergencias locales documentadas: (a) escala sobre valor absoluto del compuesto (no variación interanual); (b) indicador principal compuesto "Confianza institucional" = promedio simple de confianza-partidos + congreso + judicial, calculado en runtime; (c) `noData` estricto por país-ola sin fallback `getLastWaveBefore` (divergencia explícita vs Spec 44 que sí usó fallback semanal); (d) etiqueta bucket 0 = "Vacío institucional". B.4 hereda sin tensión — el bloque "Dirección" de la leyenda se reformula localmente como bloque "Lectura" porque la magnitud absorbe toda la lectura (sin dirección separable). 4 subindicadores: los 3 componentes del compuesto + `elecciones-fraudulentas` (eje Desorientación, `invertGood: true`) como mirada heterodoxa al cluster instituciones-formales. 14 decisiones técnicas cerradas en r1, 1 decisión visual abierta (#15 glyph SVG `presion.svg`) en Anexo A. Pre-requisito operativo declarado en frontmatter: curar 4 olas históricas LB (2018, 2020 modo crisis COVID, 2021, 2023) además de la 2024 ya hidratada — ~1 semana de trabajo análogo al `build_indicators.py` actual. Hallazgo del pre-flight relevante: el pipeline LB del proyecto era una sola ola (2024) — no serie multi-año como asumía la lectura inicial del epic. La spec convierte esa realidad en pre-requisito operativo explícito, análogo a los de Spec 43 (OIT) y Spec 44 (cobertura coding). **Spec 45 cierra el bloque "diseño de capas"** del epic — los próximos pasos son operativos (pre-requisitos de Spec 43/44/45) + diseñar Spec 46 (leyenda + onboarding) y Spec 47 (tooltip multi-capa) que asumen 2+ capas implementadas en producción | El método de "patrón heredado + divergencias locales documentadas" que Spec 43 inauguró y Spec 44 escaló (a 5 divergencias) llega acá a su forma más pulida: 4 divergencias, cada una con razón explícita arraigada en la naturaleza del dato Latinobarómetro (encuesta de niveles, no de variaciones; 5 olas discretas no continuas; gaps reales de cobertura). El brief para Product Design es chico (un glyph, sin sub-decisiones acopladas) — más chico que Spec 44 y similar a Spec 43. Spec 45 también consolida un nuevo patrón heredable: "compuestos calculados en runtime en `lib/layers/<id>.ts` sin pre-procesar al JSON" — replicable si futuras capas necesitan compuestos. Y otro patrón nuevo: "3 de 4 subindicadores como componentes desagregados del principal" — habilita una lectura "ver de qué se compone el principal" directamente en el drawer. El sistema de glyphs queda pendiente de su cuarto eje compositivo (decisión #15 abierta) — los tres consumidos son h-cerrado (precipitación), v-cerrado (temperatura), h-abierto (viento); el cuarto eje libre se decide en sesión de Product Design |
| 2026-05-21 (r10 del epic) | Spec 44 bumpea a borrador-r2 al cerrar la decisión #18 acoplada (18.a glyph + 18.b mecánica de orientación) en sesión de Product Design (Mapa Inestable Design System · spec44/index.html). Elegidos **P.1** (onda + punta direccional) sobre 4 candidatos alternativos (P.2 flecha + estela, P.3 veleta vertical, P.4 chevron desacoplado, P.5 molinete rotacional) y **V3** (asimetría intrínseca + neutro distinto, pro-estado vía `transform: scaleX(-1)` por el componente del mapa) sobre 4 mecánicas alternativas (V1 variantes separadas, V2 rotación 180° pura, V4 glyph + indicador desacoplado, V5 glyph compuesto continuo). 2 SVGs entregados al vault como SSOT: `viento.svg` (activo, onda 3-humps + extensión con punta + 2 mini-ondas secundarias) + `viento-neutro.svg` (variante propia para `rank=0`: mini-ondas reemplazadas por dashes estáticos + onda principal sin punta; significado editorial "con dato pero sin movimiento direccional"). README de glyphs actualizado con la nueva mecánica V3 y la consolidación del sistema. **El sistema de glyphs queda con 3 ejes compositivos distintos** que las cuatro capas ocupan sin colisión: horizontal cerrado (precipitación, nube + gotas), vertical cerrado (temperatura, termómetro + marcas), horizontal abierto (viento, onda + punta). El **vocabulario de ondas/sinusoides queda consumido por esta capa** — Spec 45 (presión) hereda restricción: no reusar ondas; los vocabularios disponibles documentados en README (anillos/círculos concéntricos, barras, asterismos, contornos verticales más altos que el termómetro, eje vertical abierto). AC15 cerrado del lado del SSOT; AC16 con mecánica V3 definida — sincronización al frontend y verificación visual pendientes para sesión VS Code. El epic queda con 2 specs implementables condicionales al pre-requisito operativo (43 hidratar c7 OIT; 44 cobertura mínima de coding), 1 spec todavía sin diseñar (45 presión), **0 specs con decisiones bloqueantes pendientes** | El brief #18 era acoplado (motivo + mecánica) y se cerró en una sesión. P.1 fue la única propuesta que (a) materializó el vocabulario reservado de ondas (caveat heredado de Spec 43 r2 cuando T.4 fue descartado precisamente para preservarlo acá), (b) aportó un eje compositivo nuevo al sistema sin colisionar con los existentes, (c) cargó la dirección sin texto a 24px gracias a la asimetría intrínseca de la punta sobre el flujo de la onda. V3 fue la mecánica que mejor preservó la unidad visual del glyph (vs V4 que rompe la unidad con un indicador desacoplado) y la honestidad del neutro (no es "versión apagada" del activo sino glyph con cuerpo propio: dashes en vez de mini-ondas, onda sin punta), evitando duplicar trabajo de mantenimiento (vs V1 con 3 SVGs separados) y preservando la asimetría intrínseca (vs V2 rotación pura que la habría descartado). La sesión cerró todas las decisiones del epic — Spec 44 ya no bloquea ningún diseño y se suma a Spec 43 en el grupo "lista para handoff a VS Code, condicional al pre-requisito operativo" |
| 2026-05-21 (r12 del epic) | Specs 46 y 47 creadas en borrador-r1 en una sola sesión de Cowork. **Spec 46 (sistema de leyenda + onboarding visual)** cierra 15 decisiones técnicas con onboarding como eje central (guided carousel modal de 4 pantallas: bienvenida → 4 capas → modelo de tiempo → despedida) en lugar de tour contextual con popovers numerados (descartado por romper la dirección estética Grabado), chip persistente `<LayerGlossaryChip>` en esquina inferior izquierda con glosario climático ↔ político, refinamientos r2 a la leyenda (badge de calidad de 3 estados oficial/estimado/congelado, microcopy didáctico debajo de la escala que aclara qué codifica el color — necesario porque B.4 puede leerse como bug visual sin nota, accordion `[▼ Cómo se lee esta capa]` con `shortIntro` 2-4 oraciones), extensión del contrato `Layer` con dos campos opcionales nuevos (`legendMicrocopy: string` y `shortIntro: string` — cada capa los aporta, degradación silenciosa si vacíos), persistencia versionada en `localStorage` con clave `mi.onboarding.v1.seen`, único punto de re-entrada via `[ⓘ Cómo se lee este mapa]` al pie de la leyenda. 1 decisión visual abierta (#16 header glyph compuesto de pantalla 1: H.1 línea horizontal / H.2 composición ensamblada / H.3 glyph compuesto nuevo / H.4 solo tipografía). **Spec 47 (tooltip enriquecido + interacción multi-capa)** cierra 16 decisiones técnicas **confirmando single-select como ancla del epic** (cierra la decisión condicional #4 sin romperla) y resolviendo la necesidad multi-capa vía tooltip enriquecido con snapshot cruzado en lugar de multi-activación. Tres piezas nuevas: (a) tooltip enriquecido on-hover ~320px con bloque "Otras capas" — N-1 chips compactos con mini-glyph + label corto + valor + delta o badge de calidad, donde el chip de viento usa el mini-glyph V3 orientado (Spec 44 r2 §18.b) en lugar de flecha de tendencia; (b) panel pineado persistente debajo de la leyenda al ejecutar `[📌 Pinear lectura de este país]`, que se actualiza al mover el slider y se mantiene al cambiar de capa activa; (c) bloque desplegable "Otras capas para este país" en el header del drawer con links `[Cambiar a esta capa →]` que activan la capa nueva sin perder el drawer del país. Comparación país×país queda explícitamente fuera de scope r1. Tres patrones nuevos consolidados al sistema: (i) extensión del contrato Layer con `formatCrossLayerChip?(value): LayerChipFormat` opcional + helper reutilizable `getCrossLayerSnapshot(slug, sliderDate)` en `lib/cross-layer.ts` con memoización por `(slug, sliderDate)` — primitivo que Spec 48 (risk management) puede consumir para export multi-capa; (ii) persistencia versionada del país pineado en `localStorage` (clave `mi.mapa.pinned-country.v1`, opt-in entre sesiones, decisión #7 deja abierta la posibilidad de desactivar); (iii) mobile (≤767px) no renderiza tooltip; tap abre directo el drawer con el bloque "Otras capas" expandido por default — coherente con A.4 de Spec 42. 1 decisión visual abierta (#17 diferenciación del chip cruzado: D.1 solo glyph / D.2 glyph + borde lateral cromático / D.3 banda de fondo apenas teñida / D.4 solo tipografía + glyph). El epic queda con **4 specs implementables condicionales** a pre-requisitos operativos o Anexo A (43 hidratar c7 OIT; 44 cobertura mínima de coding; 45 curación 4 olas LB; 46+47 sesiones de Product Design para los dos Anexos A), 0 specs sin diseñar, **0 specs con decisiones bloqueantes pendientes**. Los próximos pasos del epic son operativos (cobertura de datos, sesiones de Product Design) + Spec 39B (página dedicada por capa, se diseña con material editorial real de las capas implementadas) | Tomás eligió las tres opciones recomendadas en la clarificación previa: (1) "onboarding eje central + leyenda r2 con refinamientos menores" sobre dos alternativas (rediseño completo de leyenda; solo onboarding sin tocar leyenda); (2) "single-select + tooltip cruzado" sobre dual-layer split-screen y dual-layer bivariate; (3) "comparación país×país fuera de scope r1". Las tres elecciones preservan el ADN del epic: coherencia visual cerrada por Specs 42-44 intacta, decisión #4 del epic confirmada como ancla, scope acotado. Spec 46 materializa el principio "no esconder los trucos del mago" (Spec 39 r2) en el extremo de primer contacto del lector, complementando a Spec 39B (lectura profunda) — el sistema cubre los dos polos del recorrido. Spec 47 resuelve la decisión condicional #4 con un mecanismo conceptualmente más simple que la multi-activación (un primitivo helper N-agnóstico) y reusa toda la infraestructura existente (Layer, A.4, B.4, V3, onboarding/glosario de Spec 46). Ambas specs r1 dejan exactamente 1 decisión visual cada una para Product Design — patrón Spec 43 (un solo glyph) más que Spec 42/44 (decisiones acopladas). Tiempo total de diseño: ~1 hora de Cowork |

---

## Preguntas resueltas

| # | Pregunta original | Resolución | Dónde se cerró |
|---|---|---|---|
| 2 | ¿Las capas se publican como una sola release o de a una? | **De a una, en orden: precipitación → temperatura → viento → presión.** Cada capa permite iterar el patrón antes de aplicarlo a la siguiente. Spec 42 es el caso piloto que valida arquitectura | Confirmado en planificación de orden de Specs 42-45; explícito en "Próximos pasos" de este doc |
| 4 | ¿Qué pasa cuando una fuente no actualiza? | **Tres mecanismos combinados**: (a) `quality: "estimado" / "congelado"` por datapoint, (b) campo opcional `frozen_since?: string` a nivel de indicador cuando la fuente entera deja de actualizar, (c) en la leyenda del mapa se muestra "⚠ dato congelado" cuando aplica. El render no muestra blanco — siempre la última lectura disponible con su fecha explícita | Spec 40 r2, decisiones #6 y #13 |

## Preguntas abiertas

1. **¿Va el tier B2B pago?** El framing de risk management abre la posibilidad de un producto pago. Si va, Spec 48 cobra prioridad y aparece una decisión sobre modelo de cuentas / billing. Si no, el modo risk es solo una lectura del mismo producto gratuito. No hay que resolverla para arrancar las capas, pero conviene saberlo antes de Spec 48.

2. **¿Hay relación entre las capas y los 6 ejes editoriales?** Cada capa probablemente "activa" un eje (presión = erosión de mediaciones; viento = desrepresentación o estetización según la dirección; temperatura = condiciones materiales que cruzan deculturación). Vale la pena mapearlo explícitamente — habilita una feature futura: filtrar el corpus editorial por capa, o ver qué análisis se escribieron mientras una capa estaba en cierto estado. Re-evaluar cuando haya 2+ capas implementadas y aparezca uso real.

3. **¿Hay sentido en agregar una quinta capa "atención" en el futuro?** El eje de atención es transversal a todos los demás. Podría visualizarse como intensidad de cobertura mediática por país (volumen de noticias, dispersión de fuentes, concentración de temas). No para v1, pero vale notarlo.

4. ~~**¿Subcapas / sub-indicadores por capa? (apertura 2026-05-19)**~~ **Cerrada completamente en Spec 42 r2 (2026-05-20).** Dimensión técnica: contrato `Layer` se extiende con `subIndicators?: LayerSubIndicator[]` opcional (propuesto para r3 de Spec 39). Dimensión visual: **A.4** del Anexo A — subindicadores solo en el reading drawer (sparklines de 8 períodos con `invertGood` para inflación/deuda); no en hover ni en controles del LayerController. Patrón cerrado como ancla para Specs 43-45.

---

## Notas

- Este documento se actualiza con cada decisión cerrada (mover de "Decisiones abiertas" a "Decisiones cerradas" con fecha y rationale), cada spec mergeada (actualizar columna estado en la tabla), y cada pregunta abierta resuelta.
- Para detalle del mapa Torres García sobre el que se monta este epic: Spec 22.
- Para indicadores ya curados que pueden alimentar las capas: Specs 12A (12 indicadores) y 14A (24 indicadores).
- Para el patrón de calendario de agentes que inspiró este formato de epic: Spec 29.
